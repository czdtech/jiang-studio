# 任务完成后的检查清单

## 代码质量检查

### TypeScript 类型检查
```bash
# 运行 TypeScript 编译器检查类型错误
npx tsc --noEmit
```

### 构建测试
```bash
# 确保代码可以正常构建
npm run build
```

### 开发服务器测试
```bash
# 启动开发服务器确保没有运行时错误
npm run dev
# 在浏览器中访问 http://localhost:3000 进行功能测试
```

## 功能测试检查

### 核心功能验证
- [ ] 各个 AI 服务页面可以正常加载
- [ ] 图像生成功能正常工作
- [ ] 图像编辑功能正常工作
- [ ] 作品集保存和删除功能正常
- [ ] 模态框打开和关闭正常
- [ ] 供应商配置保存正常

### UI/UX 检查
- [ ] 响应式布局在不同屏幕尺寸下正常
- [ ] 深色主题样式正确应用
- [ ] 交互动画和过渡效果正常
- [ ] 错误提示和加载状态正确显示

## 代码规范检查

### 文件结构
- [ ] 新文件放在正确的目录中
- [ ] 导入路径使用正确的别名 (@/)
- [ ] 文件命名遵循项目约定

### TypeScript 规范
- [ ] 所有新增的接口和类型都有适当的类型注解
- [ ] 没有使用 `any` 类型
- [ ] 枚举和常量使用正确的命名约定

### React 规范
- [ ] 组件使用函数式写法
- [ ] Props 接口正确定义
- [ ] 状态管理使用适当的 Hook
- [ ] 副作用使用 useEffect 正确处理

## 环境变量检查
- [ ] 确认 .env.local 文件配置正确
- [ ] GEMINI_API_KEY 已设置且有效

## Git 提交前检查
```bash
# 检查文件状态
git status

# 查看更改内容
git diff

# 添加文件到暂存区
git add .

# 提交更改
git commit -m "描述性的提交信息"
```

## 部署前最终检查
- [ ] 生产构建成功: `npm run build`
- [ ] 预览构建结果: `npm run preview`
- [ ] 所有环境变量在生产环境中正确配置
- [ ] API 密钥和敏感信息不会暴露在客户端代码中